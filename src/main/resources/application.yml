logging:
  level:
    org:
      springframework: debug

management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  h2:
    console:
      enabled: true
  datasource:
    url: "jdbc:h2:mem:testDB"
  jpa:
    defer-datasource-initialization: true
    show-sql: true